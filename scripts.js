function expand(event){
    const toExpand = document.querySelector('article');

    toExpand.classList.toggle('active');
}

function close(event) {
    const toExpand = document.querySelector('article');

    toExpand.classList.toggle('active');
}